<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:orientation="vertical"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".layout.AddSubcategoryActivity"
    tools:showIn="@layout/activity_add_subcategory">

    <EditText
        android:id="@+id/inputSubcatName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Subcategory Name"/>

    <Spinner
        android:id="@+id/dropdownCatList"
        android:layout_width="match_parent"
        android:layout_height="40dp"/>

    <CheckBox
        android:id="@+id/subcatIsActive"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:checked="true"
        android:visibility="gone"
        android:text="@string/is_active"/>

</LinearLayout>